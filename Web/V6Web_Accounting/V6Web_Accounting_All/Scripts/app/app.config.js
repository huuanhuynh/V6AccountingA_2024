import {RoutesConfig} from './configs/routes.config';
import {SatellizerConfig} from './configs/satellizer.config';

angular.module('app.config')
    .config(RoutesConfig)	
	.config(SatellizerConfig)
	;
